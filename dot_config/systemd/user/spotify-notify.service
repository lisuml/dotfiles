[Unit]
Description=Spotify track change notifications via playerctl
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/spotify-notify
Restart=always
RestartSec=3

[Install]
WantedBy=graphical-session.target
